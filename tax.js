/**
 * Tax configuration object.
 * 
 * @typedef {Object} Tax
 * @property {number} minDeclaration - The minimum declaration amount.
 * @property {number} rate - The tax rate.
 */
export const Tax = {
    minDeclaration: 10,
    rate: 0.15
}